name: Validate pull request

on: [pull_request]

jobs:
  test: 
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v3
    
    - name: Install Stryker
      run: dotnet tool install -g dotnet-stryker

    - name: Run Stryker Domain
      run: dotnet stryker -tp ./templates/tests/UnitTests/ -f ./templates/tests/UnitTests/stryker-config-domain.json

    - name: Publish Report
      uses: turing85/publish-report@v2
      with:
        checkout: true
        download-artifact-name: mutation-tests-domain-result
        report-path: '**/reports/mutation-tests-domain-result.html'

    - name: Run Stryker Application
      run: dotnet stryker -tp ./templates/tests/UnitTests/ -f ./templates/tests/UnitTests/stryker-config-application.json

    - name: Publish Report
      uses: turing85/publish-report@v2
      with:
        checkout: true
        download-artifact-name: mutation-tests-application-result
        report-path: '**/reports/mutation-tests-application-result.html'

    - name: Start Docker Compose
      run: docker compose -f ./templates/docker-compose.yml up -d && sleep 10

    - name: Test
      run: dotnet test ./templates

  build:
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v3

    - name: Build Template Pack Project
      run: dotnet build -c Release
      
    - name: Build Template Solution
      run: dotnet build ./templates -c Release
